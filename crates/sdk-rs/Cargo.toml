[package]
name = "trustnet-sdk"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Rust SDK for TrustNet - fetch and verify proofs with k-of-n helpers"

[dependencies]
trustnet-core = { path = "../core" }
trustnet-smm = { path = "../smm" }
